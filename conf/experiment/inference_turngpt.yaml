name : inference_turngpt
model_name : gpt2

load:
  pretrained_model_name_or_path : gpt2
  model_head : LMHead

dataset:
  start_conv_no : 0
  end_conv_no : -1

inference:
  N : -1
  context_buffer_size : 512

